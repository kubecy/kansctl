## 

deploy_host: "moone_prod"

docker_repo:
  host: "registry.cn-guangzhou.aliyuncs.com"
  image_project: "kansctl"
  image_version: "6.2.18"


storage:
  redis_data: "/data"



service:
  redis_single: "{{ docker_repo.host }}/{{ docker_repo.image_project }}/{{docker_repo.image_version}}"
