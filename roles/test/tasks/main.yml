---
# tasks file for test

- name: Creates yaml/redis directory
  file:
    path: yaml/redis
    state: directory

- name: Copying over Redis files for services
  template:
    src: "{{ item }}.j2"
    dest: "yaml/redis/{{ item }}"
    mode: "0660"
  with_items:
    - "deployment.yaml"

