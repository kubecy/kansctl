---
apiVersion: v1
kind: Service
metadata:
  name: sonar-sonarqube
  namespace: devops
  labels:
    app: sonarqube
    release: sonar
spec:
  type: ClusterIP
  ports:
    - port: 9000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: sonarqube
    release: sonar
